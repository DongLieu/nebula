package ido_test

/*
import (
	"testing"

	keepertest "github.com/nebula-labs/nebula/testutil/keeper"
	"github.com/nebula-labs/nebula/testutil/nullify"
	"github.com/nebula-labs/nebula/x/ido"
	"github.com/nebula-labs/nebula/x/ido/types"
	"github.com/stretchr/testify/require"
)

func TestGenesis(t *testing.T) {
	genesisState := types.GenesisState{
		Params: types.DefaultParams(),

		// this line is used by starport scaffolding # genesis/test/state
	}

	k, ctx := keepertest.IdoKeeper(t)
	ido.InitGenesis(ctx, *k, genesisState)
	got := ido.ExportGenesis(ctx, *k)
	require.NotNil(t, got)

	nullify.Fill(&genesisState)
	nullify.Fill(got)

	// this line is used by starport scaffolding # genesis/test/assert
}
*/
